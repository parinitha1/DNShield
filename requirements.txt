# DNS Shield — Python dependencies
# Install with:  pip install -r requirements.txt

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Machine learning
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.2.0
joblib>=1.3.0

# Packet capture (requires libpcap; run as root)
scapy>=2.5.0

# Database
pymongo>=4.7.0

# Data validation (FastAPI dependency)
pydantic>=2.7.0
